# private/ (EN)

Personal configs & licensed assets live here.

- Duplicate `config.yaml` into this folder for secret tokens or per-home overrides.
- Drop proprietary icons, certificates, etc.; reference them explicitly.
- Ensure `.gitignore` includes `private/` before committing.
