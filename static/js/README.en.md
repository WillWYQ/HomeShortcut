# static/js/ (EN)

Plain JavaScript for dashboard behavior.

- `main.js` handles:
  - clock tick + language/theme toggles
  - polling `/api/status` & `/api/weather`
  - internet pill, alerts panel, service table, weather/aurora chips
- No bundlers/frameworks; keep dependencies zero for offline use.
