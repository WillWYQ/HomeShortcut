# static/css/ (EN)

Theme + layout rules.

- `main.css`: header, cards, table, hourly forecast, aurora badges, responsive tweaks.
- Uses system fonts + CSS variables; no external font/CDN.
- Append new sections at the end; prefer BEM-ish class names over element selectors.
