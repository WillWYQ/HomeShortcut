# static/ (EN)

Offline-first front-end assets.

- `css/` → terminal-inspired layout (no external fonts)
- `js/` → vanilla scripts for clock, status/weather polling, alerts
- Put extra icons/images here or under `private/` if sensitive
